interface:
  display_name: "Long Task Orchestrator"
  short_description: "Run constrained dev, validation, and patches."
  default_prompt: "Use $long-task-orchestrator only after an execution_ready Planning Handoff to complete constrained implementation or patch work and automated validation handoff, while never creating or writing the formal acceptance record."
