interface:
  display_name: "Testing Layer Runtime"
  short_description: "Validate long evidence, UI/UX contracts, and acceptance."
  default_prompt: "Use $testing-layer-runtime for phase testing, revision-aware long handoff reuse, exact UI/UX contract and asset verification, test ordering, manual/device/server/external validation, final acceptance evidence, change triage, and release handoff. Reuse valid long automation, bind manual UI checks to PAGE, UX-SCN, ASSET revisions, and pass durable writeback/progression gates for every test item."
